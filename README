Toolchain generated by the crosstool-ng project: https://github.com/crosstool-ng/crosstool-ng

To reproduce

1) clone https://github.com/crosstool-ng/crosstool-ng
2) Copy the appropriate config file contents from the config dir to a .config file in toplevel dir
3) ./bootstrap
4) ./configure --local
5) ./ct-ng build
